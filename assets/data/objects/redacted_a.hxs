var dist:Int = 0;

// preUpdate has accuracy...
function preUpdate(elapsed:Float)
{
	dist = FlxMath.distanceBetween(chara, this);

	if (dist <= 20)
	{
		var disto:Int = Math.floor(10 / (dist + 1));

		if (disto >= 1)
			disto = 1;

		this.alpha = disto;
	}
}
